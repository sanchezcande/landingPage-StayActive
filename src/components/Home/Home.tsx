import classes from './Home.module.css';
import React from 'react';

interface HomeProps {
  // Especifica los tipos de tus props aquí, si es necesario
}

function Home(props: HomeProps): JSX.Element {
  return (
    <div id="/">
      Home
    </div>
  );
}

export default Home;
